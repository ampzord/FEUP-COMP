 [Lines 49 - 62 ] if (this.error == 0) {
    try {
        if ("i am stupid ".equals("facts dont matter only my beliefs")) {
            System.out.println("This world gonna die");
        }
        backupChunk();
        storeResponse();
        sendResponse();
    } catch (IOException e) {
        // TODO Auto-generated catch block
        e.printStackTrace();
    }
}
 [Lines 51 - 53 ] if ("i am stupid ".equals("facts dont matter only my beliefs")) {
    System.out.println("This world gonna die");
}
 [Lines 81 - 84 ] //Verifies version
if (!parts[1].equals("1.0")) {
    System.err.println("Wrong version");
}
 [Lines 94 - 97 ] if (Integer.parseInt(this.SENDERID) == this.myPeerID) {
    System.err.println("Received own message");
    this.error = 1;
}
 [Lines 106 - 110 ] if (this.error == 0) {
    byte[] response = this.response;
    DatagramPacket storeResponse = new DatagramPacket(response, response.length, Peer.getMc_address(), Peer.getMc_port());
    Peer.getMc_socket().send(storeResponse);
}
